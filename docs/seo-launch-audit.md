# PhysioDanali — SEO & Launch Audit (100% Complete;)

*Multi-agent adversarial audit — 40 findings, 0 refuted, **40 verified**. Focus: μηδενική απώλεια SEO στο cutover.*

## 🎯 Ετυμηγορία

Honest answer: the migration foundation is strong, but as configured TODAY you WOULD lose meaningful SEO at cutover — not from hard breakage (the 264-URL redirect map resolves, nothing 404s, dual sitemaps + a legacy sitemap for fast recrawl are in place, schema/hreflang are well-architected), but from three fixable failure modes. (1) Equity dilution: 60% of legacy URLs (159/264) funnel into the generic /therapies hub — including ~40 condition pages that ranked for long-tail queries — which Google treats as soft-404-style redirects and drops; and ~15 already-rebuilt 4,000-6,500-word articles plus the shoulder/knee head-terms are mis-pointed at list/hub pages instead of their 1:1 replacements that are already live. (2) Local coverage regression: Alimos + Nea Smyrni landers and the flagship /neuro-rehab hub were never built, and the /lymphatic-glyfada niche lander is missing, so geo/head-term relevance collapses into generic hubs. (3) Operational risk: every canonical/sitemap/robots value hardcodes physiodanali.gr, which still serves the OLD WordPress site; GSC is not verified in production; there is zero analytics; and the vercel.app preview host is fully indexable — so the cutover is not yet atomic and you would be flying blind exactly when monitoring matters. The good news: almost every fix is trivial (repoint redirect rows, set env vars, unify one hours value, one title change) and nothing hard-404s. Do the ranked seoLossRisks below before flipping DNS and this becomes a genuinely low-risk, same-domain migration.


## 🔴 Κίνδυνοι απώλειας SEO — ΠΡΙΝ το cutover (κατά σειρά)

> Αυτά είναι το #1. Αν διορθωθούν πριν γυρίσει το DNS, η μετάβαση γίνεται genuinely low-risk.

**1. [BLOCKER] Non-atomic cutover. Every canonical, hreflang, JSON-LD @id/url, both sitemap <loc>, robots Host and og:image hardcode https://physiodanali.gr (src/lib/seo.ts:3 SITE_URL), which STILL serves the old WordPress site. The new app is declaring a canonical/sitemap domain it does not yet control. A partial cutover (DNS flipped but sitemaps not resubmitted, or the preview left as the only crawlable copy) causes an index gap.**
→ *Fix:* Run the entire cutover in one maintenance window: attach apex+www in Vercel with one canonical redirect, point DNS to Vercel, immediately confirm https://physiodanali.gr returns the NEW app (200, self-referential canonical), then resubmit BOTH /sitemap.xml and /sitemap-legacy.xml in GSC. Design is correct end-state; this is purely an operational gate. Same-domain = NO Change of Address tool.

**2. [HIGH] GSC HTML-tag verification is ABSENT in production. GOOGLE_SITE_VERIFICATION is unset in Vercel (layout.tsx:61 gates it; live HTML has no verification meta). Without a verified property you cannot submit either sitemap to force recrawl of the 264 redirects, nor monitor Coverage during migration — exactly when you need it most.**
→ *Fix:* Set GOOGLE_SITE_VERIFICATION in Vercel production AND add a GSC DNS-TXT property (survives the DNS flip). Confirm the meta tag renders on the live .gr domain before cutover.

**3. [HIGH] Preview host physiodanali.vercel.app is fully indexable (robots Allow:/, no X-Robots-Tag, no meta noindex) while every canonical points to physiodanali.gr (old site). Google may distrust the cross-domain canonical and index the vercel.app duplicate, splitting/leaking signals pre-cutover; og:image also resolves against the old site.**
→ *Fix:* Emit X-Robots-Tag: noindex when the request Host is *.vercel.app (or enable Vercel deployment protection) until DNS cutover; then ensure Googlebot stays unblocked on physiodanali.gr. Note the robots.txt 'Host:' line is Yandex-only and gives Google no protection.

**4. [HIGH] 159 of 264 legacy 301s (60%) target the generic /therapies hub, including ~40 topically-specific condition/treatment pages (plantar-fasciitis, frozen-shoulder, meniscus, carpal-tunnel, achilles-tendinopathy, all the *-fractures) that ranked for long-tail queries. /therapies has no matching content, so Google treats these as soft-404-style redirects and drops the accrued rankings — the single largest ranking-loss vector in the migration.**
→ *Fix:* Pull 12-mo GSC Pages+Queries for the old site; for each condition URL that earns impressions, either build a dedicated page (existing pattern: /low-back-pain, /neck-pain, /hip-pain, /kyphosis) or add named-anchor sections on /therapies and redirect to /therapies#<condition>. Repoint those rows in src/lib/legacy-redirects.ts. Do NOT ship 60% of URL equity into one generic hub blind.

**5. [HIGH] 15 already-rebuilt articles (4,000-6,500 words, live, indexable at /articles/<slug>) exist, but their old blog 301s point at the generic /articles list or a service hub, not the 1:1 article — a soft-consolidation that dilutes equity instead of passing it to the page that should inherit it.**
→ *Fix:* Trivial repoint in src/lib/legacy-redirects.ts: blog-disc-herniation->/articles/disc-herniation, -frozen-shoulder, -rotator-cuff, -vestibular-rehab, -multiple-sclerosis, -parkinsons-exercise, -tendinopathy-or-tendonitis->/articles/tendinopathy, -elbow/foot/hand-pain, -sprained-ankle, -knee-pain, -shoulder-pain, -brazilian-drainage, -stroke-rehab+stroke-rehabilitation->/articles/stroke-rehabilitation. Cross-check the full published slug list (curl /articles) against every blog-* row first.

**6. [HIGH] The two highest-volume commercial head-terms have no dedicated page: old /el/shoulder-pain-treatment-gr.html and /el/knee-pain-treatment-gr.html plus their entire sub-clusters (frozen-shoulder, impingement, rotator-cuff, supraspinatus; meniscus, ACL, MCL-LCL, patellofemoral, ITB, knee-osteoarthritis) all 301 to generic /therapies, while rich /articles/shoulder-pain (4,382 words) and /articles/knee-pain sit unused.**
→ *Fix:* Minimum: repoint those rows to /articles/shoulder-pain and /articles/knee-pain. Better: build dedicated /shoulder-pain and /knee-pain condition pages (condition-page template with FAQPage+MedicalTherapy) as cluster landing targets, per the plan's P1 intent.

**7. [HIGH] The flagship neuro-rehab cluster (P0 in the plan, 8+ URLs incl. per-area neuro landers + neurological-physical-therapy + stroke/MS/Parkinson/cerebral-palsy) was never built (no src/app/neuro-rehab) and its surviving URLs are split across two unrelated hubs — physical-therapy-neuro-at-home-gr->/home-care but neurological-physical-therapy-gr->/therapies — so no single page accumulates the cluster's authority for the brand's post-stroke at-home service.**
→ *Fix:* Build /neuro-rehab and repoint the whole neuro cluster to it, interlinking the live stroke/MS/Parkinson articles. At minimum, make every neuro-* legacy row target ONE destination instead of splitting between /home-care and /therapies.

**8. [HIGH] Alimos and Nea Smyrni have NO area landers (physio+chiro) though Alimos is marketed (areaServed, homepage hero, llms.txt) and both were dedicated ranking pages on the old site. Their legacy URLs 301 to generic /home-care and /chiropractic, dropping the geo-specific keyword targeting ('φυσικοθεραπεία Άλιμος', 'χειροπρακτική Νέα Σμύρνη'). Glyfada/Voula/Vari/Vouliagmeni all got bespoke landers — these two are the sole gap.**
→ *Fix:* If still served (client must confirm), build the four landers via the src/lib/area-landers.ts pattern and repoint legacy-redirects.ts rows 21/23/29/30 (+en/neuro variants) to them; add Νέα Σμύρνη to areaServed in src/lib/seo.ts. If not served, confirm the loss is intentional.

**9. [HIGH] Every area-lander <title> double-prints the brand ('…στη Γλυφάδα — PhysioDanali · PhysioDanali') because meta.title already ends with the brand and layout's template re-appends it — across all 7 EL+EN landers. Wastes SERP pixels, risks truncating the locality keyword, and reads as an error: a CTR drag on the exact local-intent queries the old site ranks 5.0★ for.**
→ *Fix:* Return { title: { absolute: data.meta.title } } from each area-lander generateMetadata, OR strip '— PhysioDanali' from the title strings in src/lib/area-landers.ts.

**10. [HIGH] Opening-hours conflict published to Google AND answer-engines. Everything canonical says 08:00-23:00 (LocalBusiness schema seo.ts:49-50, UI, GBP), but public/llms.txt (3 places) and the FAQ_HOURS_EL/EN constants in src/lib/area-landers.ts say 10:00-22:00 — and those wrong hours are emitted inside FAQPage JSON-LD on all 7 landers (SERP rich-result risk) and are what ChatGPT/Perplexity/Claude quote from llms.txt.**
→ *Fix:* Change public/llms.txt and FAQ_HOURS_EL/FAQ_HOURS_EN to 08:00-23:00 so schema, area-page FAQPage, UI, GBP and LLMs all state identical hours before cutover.

**11. [HIGH] aggregateRating is self-serving (no nested Review objects anywhere in the graph) and understated/inconsistent: schema reviewCount 100 (seo.ts:61) vs /reviews page 121 vs real GBP ~122. Google will not render review stars for a self-serving LocalBusiness rating lacking Review markup, so star rich results are forfeited, and three different counts is a consistency signal problem. The block is also injected globally onto non-review pages (articles/contact).**
→ *Fix:* Set one true count (client confirms, ~122) as a single source of truth referenced by seo.ts and the /reviews page; emit schema.org Review objects from the 13 real testimonials already on /reviews (QUOTES_EL/EN) so the rating is rich-result eligible; and/or scope the aggregateRating off non-review pages.

**12. [MEDIUM] Homepage <title> dropped the city + '(έως 23:00)' hooks the old CTR-tested title carried ('…στη Βούλα (έως 23:00)'). The new brand-led 'PhysioDanali — Χειροπρακτική και Φυσικοθεραπεία κατ' οίκον' has no locality or availability on the site's highest-authority page (interior landers keep locality — this is homepage-specific).**
→ *Fix:* Reintroduce locality+availability into the homepage title in src/app/layout.tsx, e.g. '… κατ' οίκον στη Γλυφάδα & Βούλα — έως 23:00', keeping the brand.

**13. [MEDIUM] ~28-30 English blog posts all 301 to /en/therapies. The new blog is Greek-only (/en/articles lists 0 posts), so the entire EN informational long-tail — an affluent expat audience the site explicitly targets in Glyfada/Vouliagmeni — is abandoned rather than migrated.**
→ *Fix:* Decide from GSC (plan open question #10): translate the top EN articles (the AI CMS supports it) and repoint each EN blog 301 to /en/articles/<slug>, or confirm EN blog traffic is negligible and accept the loss. Repointing to /en/articles today would not help — it has no content yet.

**14. [MEDIUM] EN area-lander pages emit GREEK-language JSON-LD: the page body hardcodes getAreaLander('el', SLUG) for the MedicalTherapy/FAQ/breadcrumb schema even though metadata is correctly localized. On all 7 /en landers the FAQ rich-result content and procedure name mismatch the page language.**
→ *Fix:* Use getAreaLander(await getLocale(), SLUG) in each area-lander Page(). Note faqSchema also hardcodes inLanguage 'el-GR' (seo.ts:154), so tag the FAQ language by locale too.

**15. [MEDIUM] /lymphatic-glyfada niche geo-lander (P1; high-margin post-liposuction service recently invested in; Glyfada is the cosmetic-surgery clientele hotspot) was not built; the area lymphatic URLs collapse to generic /lymphatic (legacy-redirects.ts:31,43,52), losing the geo-qualified 'λεμφικό drainage Γλυφάδα' term that converts for that audience.**
→ *Fix:* If the post-lipo lymphatic service is actively marketed, build /lymphatic-glyfada via the area-landers pattern and repoint the Glyfada/Vari lymphatic rows; otherwise confirm the /lymphatic consolidation is intentional with the owner.

**16. [MEDIUM] Every page is served cache-control no-store (x-vercel-cache MISS) — even static marketing pages never hit Vercel's edge cache, raising TTFB/LCP (a Core Web Vitals component Google measures) vs the edge-cached old WordPress site. Root cause is the root layout reading dynamic APIs (src/app/layout.tsx:32 headers(), src/lib/i18n-server.ts headers()+cookies()), which opts the whole tree into dynamic rendering — NOT a middleware cookie (proxy.ts only sets a cookie on /en).**
→ *Fix:* Remove headers()/cookies() from the root-layout render path (resolve locale/pathname without dynamic APIs at the root) so static routes return cacheable headers. Verify with a repeat curl -I that /about returns a cacheable cache-control.

**17. [LOW] /en/physical-therapy-at-home-gr.html 301s to the Greek /home-care instead of /en/home-care (legacy-redirects.ts:66), landing an EN-indexed URL and Googlebot on Greek content and breaking hreflang continuity at the redirect target.**
→ *Fix:* Change the target to /en/home-care (the sibling -eng.html row already does this correctly).


## 🚀 Cutover Runbook — μηδενική απώλεια

### Α. Πριν το cutover
- [ ] GSC + baseline: verify/regain access to the existing physiodanali.gr GSC property; export the last 12 months of Pages + Queries and the full Coverage/indexed-URL list. This drives every triage decision below.
- [ ] Set GOOGLE_SITE_VERIFICATION in Vercel production AND add a GSC DNS-TXT domain property (survives the DNS flip); confirm the verification meta renders on the live domain.
- [ ] URL-coverage cross-check: confirm all 260 old sitemap URLs (131 GR + 129 EN, plus their extensionless 200 duplicates) are present in src/lib/legacy-redirects.ts and 308 correctly — any indexed URL missing from the map 404s at flip. next.config.ts already expands .html/extensionless/lowercase; spot-verify.
- [ ] Repoint the mis-targeted 301s (all in src/lib/legacy-redirects.ts): 15 blog rows -> /articles/<slug>; shoulder/knee head-terms + sub-clusters -> /articles/shoulder-pain and /articles/knee-pain; unify the neuro cluster onto one destination; /en/physical-therapy-at-home-gr.html -> /en/home-care.
- [ ] Triage the ~40 condition/treatment URLs currently going to /therapies against GSC impressions; for each ranking one, build a dedicated page or add /therapies#<condition> anchors and repoint.
- [ ] Build the missing pages the plan calls for IF the services/areas are confirmed served: /neuro-rehab (P0), Alimos + Nea Smyrni landers (P0/P1), /lymphatic-glyfada (P1); add Νέα Σμύρνη to areaServed in src/lib/seo.ts.
- [ ] Fix the on-page CTR items: area-lander title double-brand (title.absolute) and restore the homepage locality + '(έως 23:00)' hooks in src/app/layout.tsx.
- [ ] Unify opening hours to 08:00-23:00 in public/llms.txt and the FAQ_HOURS_EL/EN constants in src/lib/area-landers.ts.
- [ ] Set the real review count (single source of truth) and add schema.org Review objects from the /reviews testimonials; fix the EN area-lander JSON-LD locale (getLocale()).
- [ ] Gate X-Robots-Tag: noindex on the *.vercel.app host until DNS flips (or enable Vercel deployment protection).
- [ ] Install analytics (Plausible/GA4) and confirm it records on the new app so a post-cutover baseline exists.
- [ ] Launch-blockers unrelated to redirects: fill ΑΦΜ + license in src/lib/content.ts; verify physiodanali.gr as a Resend sender (DKIM + DMARC) or accept amox.gr short-term, and set RESEND_API_KEY/NOTIFY_TO/BOOKING_NOTIFY_TO in Vercel; replace the contact-form mailto with a real server action.
- [ ] Keep the old WordPress deployment fully retrievable for instant rollback.

### Β. Ημέρα Χ
- [ ] Attach both apex and www in Vercel with a single canonical redirect (301 www->apex or apex->www).
- [ ] Point apex + www DNS to Vercel.
- [ ] Immediately confirm https://physiodanali.gr returns the NEW Next.js app (HTTP 200), the canonical is now self-referential, and apex + www + http all 301 to the one https canonical.
- [ ] Spot-check 15-20 high-value legacy URLs live on the .gr domain (condition pages, area landers, top blog posts) return 308 -> the correct new target.
- [ ] Confirm robots.txt on .gr does not block Googlebot, and both /sitemap.xml and /sitemap-legacy.xml fetch cleanly.
- [ ] Resubmit BOTH /sitemap.xml and /sitemap-legacy.xml in GSC to force fast recrawl of the 264 redirects. Do NOT touch Change of Address (same-domain migration).

### Γ. Μετά (monitoring)
- [ ] Monitor GSC Coverage/Pages daily for spikes in 404s, soft-404s, 'crawled - currently not indexed', and redirect errors — the /therapies-consolidated URLs are the ones to watch for soft-404 drops.
- [ ] Watch GSC Performance for query/impression/click drops vs the 12-month baseline, focusing on the condition head-terms and 'φυσικοθεραπεία/χειροπρακτική + suburb' local queries.
- [ ] Run the Rich Results Test on key pages to confirm FAQPage and (once Review objects ship) star rich results are eligible and the corrected 08:00-23:00 hours appear.
- [ ] Watch analytics for a traffic cliff; compare week-over-week against the old-site baseline.
- [ ] Send a live end-to-end test booking (email deliverability) and a contact-form submission after the backend fix.
- [ ] Keep /sitemap-legacy.xml submitted ~6 months until the 301s are fully processed, then remove it.
- [ ] Retain the WordPress rollback for at least 2-4 weeks before decommissioning.


## ⚡ Quick wins — τα κάνουμε ΤΩΡΑ (χωρίς εξάρτηση)

- [ ] Repoint the 15 blog 301s to their live /articles/<slug> targets in src/lib/legacy-redirects.ts (biggest bang-for-effort SEO fix).
- [ ] Repoint the shoulder-pain / knee-pain head-terms + their sub-cluster rows to /articles/shoulder-pain and /articles/knee-pain.
- [ ] Fix /en/physical-therapy-at-home-gr.html -> /en/home-care (one-line, legacy-redirects.ts:66).
- [ ] Unify the neuro cluster redirects onto a single destination (even before /neuro-rehab is built).
- [ ] Fix the area-lander title double-brand via { title: { absolute: data.meta.title } }.
- [ ] Restore the homepage locality + '(έως 23:00)' hooks in src/app/layout.tsx.
- [ ] Unify opening hours to 08:00-23:00 in public/llms.txt and FAQ_HOURS_EL/EN in src/lib/area-landers.ts.
- [ ] Fix the EN area-lander JSON-LD to use getLocale() instead of hardcoded 'el'.
- [ ] Add BreadcrumbList to /about and /contact and a ContactPoint to /contact.
- [ ] Emit host-conditional noindex on the *.vercel.app preview host.
- [ ] Add schema.org Review objects from the 13 existing /reviews testimonials and single-source the review count.
- [ ] Fix the uncacheable-pages root cause by removing headers()/cookies() from the root layout render path.
- [ ] Add a skip-to-content link, a web manifest, and icon.png / apple-icon.png.
- [ ] Replace the contact-form mailto hack with a real Supabase+Resend server action reusing the booking pattern.

## 📋 Πλήρης λίστα για 100% (ανά area)

| Sev | Area | Owner | Τι λείπει |
|---|---|---|---|
| 🔴 BLOCKER | legal | client | Business ΑΦΜ and professional license/ΠΣΦ registry number are blank (src/lib/content.ts:31-32 afm:'' license:''); the footer renders nothing. legal.ts flags both as pre-publish TODOs. |
| 🟠 HIGH | technical-seo | us + client | GSC HTML-tag verification absent in production (GOOGLE_SITE_VERIFICATION unset in Vercel). |
| 🟠 HIGH | technical-seo | us | vercel.app preview host is fully crawlable/indexable while canonicals point to the old-site domain. |
| 🟠 HIGH | content | us + client | 159/264 legacy URLs (incl. ~40 ranking condition pages) 301 to the generic /therapies hub — soft-404 ranking-loss. |
| 🟠 HIGH | content | us | 15 rebuilt articles are live but the old blog 301s point to /articles list or hubs, not the 1:1 article. |
| 🟠 HIGH | content | us | Shoulder-pain and knee-pain head-terms + sub-clusters 301 to /therapies despite rich /articles/shoulder-pain and /articles/knee-pain existing. |
| 🟠 HIGH | local-geo | us + client | Neuro-rehab flagship hub (P0) never built and its cluster is split across /home-care and /therapies. |
| 🟠 HIGH | local-geo | us + client | Alimos + Nea Smyrni area landers missing though Alimos is marketed; legacy URLs go to generic hubs. Nea Smyrni also missing from areaServed. |
| 🟠 HIGH | local-geo | us | Opening-hours conflict: llms.txt + area-lander FAQPage schema say 10:00-22:00 while schema/UI/GBP say 08:00-23:00. |
| 🟠 HIGH | local-geo | us + client | aggregateRating understated + inconsistent (100 vs 121 vs ~122) and has no Review objects — star rich results forfeited. |
| 🟠 HIGH | functionality | us | Contact form is a mailto: hack with a fake 600ms success timeout and no backend — leads are silently lost when no mail client opens, while the UI shows success. |
| 🟠 HIGH | functionality | us + client | Booking/patient emails send from the agency domain amox.gr; physiodanali.gr has no Resend DKIM and no DMARC, so switching sender naively will bounce/spam. Also all mail silently no-ops if RESEND_API_KEY is unset. |
| 🟠 HIGH | analytics | us + client | Zero analytics installed (no GA4/Plausible/Vercel Analytics) — no instrument to detect a traffic/ranking drop at cutover, undermining the #1 no-SEO-loss goal. |
| 🟡 MEDIUM | technical-seo | us | about and contact pages have no page-level BreadcrumbList; contact has no ContactPoint schema (only the global business+person blocks). |
| 🟡 MEDIUM | technical-seo | us | EN area-lander pages emit Greek-language JSON-LD (page body hardcodes getAreaLander('el', SLUG)); faqSchema also hardcodes inLanguage 'el-GR'. |
| 🟡 MEDIUM | content | us + client | ~7 old blog slugs (carpal-tunnel, tennis-elbow, golfers-elbow, patellofemoral, peripheral-neuropathy, cerebral-palsy, plantar-fasciitis) have no rebuilt equivalent — currently redirect to /articles or /therapies with no matching content. |
| 🟡 MEDIUM | content | us + client | ~28-30 EN blog posts all 301 to /en/therapies; EN informational long-tail abandoned (EN blog is empty). |
| 🟡 MEDIUM | local-geo | us + client | /lymphatic-glyfada niche geo-lander (P1) missing; area lymphatic URLs collapse to /lymphatic. |
| 🟡 MEDIUM | local-geo | us + client | LocalBusiness sameAs omits the Google Business Profile / Maps place URL; no hasMap; contact page has no embedded map or directions link. |
| 🟡 MEDIUM | local-geo | us + client | New robots.ts reverses the owner's deliberate AI-crawler policy (allows GPTBot + Google-Extended training bots the old site blocked; uses deprecated Claude-Web; no CCBot/Bytespider block). |
| 🟡 MEDIUM | legal | us + client | Cookie/Privacy policy describes an analytics consent banner and first-visit choice that do not exist (no banner, no analytics installed). |
| 🟡 MEDIUM | performance | us | Every page served cache-control no-store (x-vercel-cache MISS) — static marketing pages never edge-cache, hurting TTFB/LCP vs the old edge-cached site. |
| ⚪ LOW | technical-seo | us | el-only pages (/articles, /privacy, /cookies, /terms) set alternates:{canonical}, which drops the languages/x-default hreflang cluster. |
| ⚪ LOW | technical-seo | us | No Content-Security-Policy enforced (next.config.ts template commented out); dangerouslyAllowSVG is true. |
| ⚪ LOW | technical-seo | us | No PWA web manifest and no PNG/apple-touch icons (only favicon.ico); /manifest.webmanifest, /icon.png, /apple-icon.png all 404. |
| ⚪ LOW | content | us | Core condition landing pages (~1,000 words) are markedly thinner than the 4,000-6,500-word articles absorbing their inbound equity, and are not cross-linked to the matching deep article. |
| ⚪ LOW | a11y | us | No skip-to-content link before the header nav (WCAG 2.4.1). |

## 🔑 Τι χρειαζόμαστε από τον πελάτη

- Google Search Console access (or confirmation of the existing physiodanali.gr property) + export of the last 12 months of Pages and Queries and the full Coverage/indexed-URL list — this is the gate for triaging which /therapies-bound and blog URLs actually rank.
- DNS control to point apex + www to Vercel at cutover, and to add the Resend DKIM CNAMEs + a DMARC record for physiodanali.gr email.
- Decision: are Alimos and Nea Smyrni still served? (build the four area landers vs. accept the geo-keyword loss).
- Decision: is the post-liposuction lymphatic service actively marketed? (build /lymphatic-glyfada vs. keep the /lymphatic consolidation).
- Decision: English blog — translate the top EN articles or accept the EN long-tail loss?
- Confirmation of the canonical opening hours (we assume 08:00-23:00 to match schema + GBP + old site).
- The real current Google review count (schema says 100, /reviews says 121, GBP ~122 — we need one number).
- The Google Business Profile / Google Maps place URL (for sameAs + hasMap + a directions link on /contact).
- Business ΑΦΜ and professional license / ΠΣΦ registry number (launch blocker — from the accountant / registry).
- AI-crawler policy decision: mirror the old site's block-training-bots stance, or keep the new site open?
- Analytics choice + consent approach (Plausible/Umami with no banner, or GA4 with a consent banner).
- Confirm the Vercel production env values to set: GOOGLE_SITE_VERIFICATION, RESEND_API_KEY, NOTIFY_TO, BOOKING_NOTIFY_TO, and approval of the email sender domain (physiodanali.gr once verified, or amox.gr short-term).

---
## Παράρτημα — Και τα 40 verified findings

### 🔴 BLOCKER · seo-migration · ⚠️SEO — All canonicals/robots Host/sitemaps hardcode physiodanali.gr, which still serves the OLD WordPress site — cutover must be atomic
- **Πρόβλημα:** The new app (physiodanali.vercel.app) emits canonical https://physiodanali.gr, hreflang el/en/x-default -> physiodanali.gr, robots Host: https://physiodanali.gr and both sitemaps under physiodanali.gr — but physiodanali.gr currently returns the OLD WordPress site. Until DNS flips, the new app is telling Google 'my canonical is a URL that serves different content.' The migration only succeeds if the DNS cutover, sitemap resubmission, and legacy-sitemap submission all happen together. Any partial state (DNS flipped but sitemaps not resubmitted, or vercel preview left as the only crawlable copy) risks an index gap.
- **Απόδειξη:** Live: curl new home shows <link rel="canonical" href="https://physiodanali.gr"/> and hreflang all -> physiodanali.gr; robots.txt Host: https://physiodanali.gr; seo.ts:3 SITE_URL. Old site still live at physiodanali.gr (RECON D).
- **Fix:** Runbook: (1) point apex + www DNS to Vercel and add both domains in Vercel with one canonical (301 www->apex or apex->www); (2) immediately confirm https://physiodanali.gr/ returns the NEW app (200, new canonical self-referential); (3) resubmit /sitemap.xml AND /sitemap-legacy.xml in GSC; (4) keep the old WordPress deploy retrievable for rollback; (5) same-domain migration = NO Change-of-Address tool needed. Do all in one maintenance window.
- **Verify (CONFIRMED):** Reproduced every claim. The live new app at physiodanali.vercel.app emits exactly: `<link rel="canonical" href="https://physiodanali.gr"/>`, hreflang el/x-default -> https://physiodanali.gr and en -> https://physiodanali.gr/en, and robots.txt with `Host: https://physiodanali.gr` plus `Sitemap: https://physiodanali.gr/sitemap.xml` and `.../sitemap-legacy.xml`. Source confirms it: src/lib/seo.ts:3 `

### 🔴 BLOCKER · legal — Business ΑΦΜ and professional license number are blank
- **Πρόβλημα:** src/lib/content.ts:31-32 has `afm: ""` and `license: ""` (license = Αρ. άδειας ασκήσεως επαγγέλματος / Μητρώο ΠΣΦ). The footer (src/components/site/footer.tsx:277-278) conditionally renders these only if non-empty, so live they are simply absent. legal.ts's own header comment (lines 8-10) flags that the client/lawyer must fill ΑΦΜ + license before publishing. For a Greek sole-practitioner healthcare business a website legally must display the ΑΦΜ, and displaying the physiotherapy license/registry number is both an E-E-A-T trust signal and effectively expected for a regulated health professional. Launching without them is a compliance gap.
- **Απόδειξη:** src/lib/content.ts:31 `afm: ""`, :32 `license: ""`; src/lib/legal.ts:8-10 pre-publish TODO comment
- **Fix:** Fill afm and license in site.legal (content.ts) with the real values before cutover. Confirm with the owner's accountant/ΠΣΦ registry number.
- **Verify (CONFIRMED):** Reproduced all evidence. content.ts:31-32 has afm:"" and license:"" (comment lines 24-27 say both ΠΡΕΠΕΙ to be filled before launch). footer.tsx:277-278 render them only when truthy, so empty values emit nothing. legal.ts:6-10 carries the pre-publish TODO for client/lawyer. Live physiodanali.vercel.app footer shows no ΑΦΜ or Αρ. αδείας; the lone "ΠΣΦ" match on the page is an unrelated stat heading

### 🟠 HIGH · content-parity · ⚠️SEO — 159 of 264 legacy URLs (60%) 301 to the generic /therapies hub — soft-404 / ranking-loss risk
- **Πρόβλημα:** grep of src/lib/legacy-redirects.ts shows 159 entries target "/therapies" or "/en/therapies". These include topically-specific pages that ranked for long-tail condition/treatment queries the old site was optimized for (e.g. /el/plantar-fasciitis-treatment-gr.html, /el/frozen-shoulder-treatment-gr.html, /el/meniscus-treatment-gr.html, /el/carpal-tunnel-syndrome-treatment-gr.html, /el/achilles-tendinopathy-treatment-gr.html, all the *-fractures pages). /therapies is a generic services overview that does not contain matching content for those conditions. Google treats a 301 to a page that is not a close topical equivalent as a soft-404-style redirect: it honors the redirect but drops the URL's accrued rankings because the destination doesn't satisfy the original query. This is the single largest ranking-loss vector in the migration.
- **Απόδειξη:** grep -cE 'target: "/(en/)?therapies"' src/lib/legacy-redirects.ts = 159; e.g. legacy-redirects.ts:216 plantar-fasciitis-treatment-gr.html -> /therapies; :213 meniscus-treatment-gr.html -> /therapies. Old GR/EN sitemaps assigned these treatment pages priority 0.7-0.9 (RECON D).
- **Fix:** Pull GSC 'Pages' + 'Queries' (last 12 mo) for the OLD site and identify which of these ~40 condition/treatment URLs actually earn impressions/clicks. For each meaningful one, either (a) build a dedicated new page (the site already has this pattern: /low-back-pain, /neck-pain, /hip-pain, /kyphosis) and repoint the 301 to it, or (b) add named anchor sections covering those conditions on /therapies and redirect to /therapies#<condition>. Do NOT ship 60% of the URL equity into a single generic hub.
- **Verify (CONFIRMED):** Reproduced every load-bearing fact. `grep -cE 'target: "/(en/)?therapies"' src/lib/legacy-redirects.ts` = 159, out of 264 total = 60% exactly as stated. Cited line numbers are exact: line 216 = /el/plantar-fasciitis-treatment-gr.html -> /therapies, line 213 = /el/meniscus-treatment-gr.html -> /therapies. Confirmed the pool includes topically-specific long-tail condition pages consolidated to the g

### 🟠 HIGH · content-parity · ⚠️SEO — ~30 blog posts/lang 301 to /articles index or /therapies; new Supabase blog is near-empty at launch
- **Πρόβλημα:** 17 blog-* sources redirect to /articles (the blog INDEX, not an article) and ~29 more to /therapies/condition hubs. Redirecting an individual indexed article to a listing/index page is a classic soft-404 pattern — Google frequently drops such URLs. Compounding this: the new blog is a Supabase CMS (sitemap.ts:70 getPublishedSlugs) that may have few or zero published articles at cutover, so there is no equivalent content receiving the redirected authority. The old blog was 2 x ~30 posts of indexed, ranking content.
- **Απόδειξη:** blog-* target histogram: 17 -> /articles, 23 -> /en/therapies, 6 -> /therapies, plus condition hubs (Bash grep). e.g. legacy-redirects.ts:101 blog-carpal-tunnel-gr.html -> /articles; :129 blog-carpal-tunnel-eng.html -> /en/therapies. /articles returns 200 but is a list page.
- **Fix:** Before cutover, recreate (or AI-migrate) the highest-traffic old blog posts as new /articles/<slug> entries and repoint each 301 to its specific new article, not to /articles. Where a post maps to a condition hub (hip/low-back/neck/lymphatic) that's fine. Verify the CMS has content published so redirected posts land on real, relevant pages.
- **Verify (ADJUSTED):** Histogram is accurate (17 blog-* -> /articles, 23 -> /en/therapies, 6 -> /therapies, plus hubs) and /el/blog-disc-herniation-gr.html does 308 -> /articles index (live-confirmed), so the redirect-to-listing pattern is real. But the finding's central premise is REFUTED: the new Supabase blog is NOT near-empty. Live /articles returns 18 published articles whose slugs map onto the old blog (disc-herni

### 🟠 HIGH · seo-migration · ⚠️SEO — Cutover prerequisites: www/apex canonicalization, GSC verification env, sitemap submission, rollback retention
- **Πρόβλημα:** Beyond DNS, several must-dos gate a lossless cutover: (1) GOOGLE_SITE_VERIFICATION is env-gated (layout.tsx) and only in .env.example — if unset in Vercel production, the GSC HTML-tag verification disappears at cutover and you lose the ability to submit sitemaps / monitor coverage exactly when you need it most. (2) Both www and apex must be added in Vercel with a single 301 to one canonical host (Vercel does apex<->www automatically only if both domains are attached). (3) Both /sitemap.xml and /sitemap-legacy.xml must be (re)submitted in GSC at cutover so Google recrawls the 264 old URLs and discovers the 301s fast. (4) Keep the old WordPress build retrievable for instant rollback. (5) Same-domain migration means NO Change-of-Address is needed (and using it would be wrong).
- **Απόδειξη:** layout.tsx:61 verification only if env set; .env.example only reference (RECON A). robots.ts:27 lists both sitemaps. sitemap-legacy.xml live with 264 locs pointing to physiodanali.gr (verified).
- **Fix:** Pre-cutover: set GOOGLE_SITE_VERIFICATION in Vercel production and confirm the meta tag renders on the live custom domain; attach both apex+www in Vercel with one canonical redirect; verify apex+www+http all 301 to the single https canonical. At cutover: resubmit both sitemaps in GSC; retain old-site rollback; do not touch Change-of-Address.
- **Verify (CONFIRMED):** Every technical claim reproduced. (1) layout.tsx:61 gates `verification` on `process.env.GOOGLE_SITE_VERIFICATION`; the only other reference is .env.example:19 (grep confirms no hardcoded token anywhere). Critically, I fetched the live https://physiodanali.vercel.app/ and found NO google-site-verification meta tag rendered — proving the env var is currently unset in Vercel production, so the findi

### 🟠 HIGH · seo-migration · ⚠️SEO — New site is fully indexable on physiodanali.vercel.app while every canonical/OG points to physiodanali.gr (old site)
- **Πρόβλημα:** Live robots.txt on the new deployment returns `User-Agent: * / Allow: /` with no meta robots and no X-Robots-Tag noindex (verified: home HTML has no robots meta; sitemap/og responses have no X-Robots-Tag). Meanwhile SITE_URL/metadataBase is hardcoded to https://physiodanali.gr, so live canonical, all hreflang, all JSON-LD @id/url, sitemap <loc>, and og:image (https://physiodanali.gr/opengraph-image) point to a domain that currently serves the OLD WordPress site. Two live risks: (1) Google can crawl/index physiodanali.vercel.app as a standalone host that self-canonicalizes to a URL it does not control; (2) cutover is not atomic unless DNS flip and any preview-noindex removal happen together. Pre-cutover, og:image also resolves against the old site so social shares of the vercel URL break.
- **Απόδειξη:** src/lib/seo.ts:3 SITE_URL="https://physiodanali.gr"; live curl https://physiodanali.vercel.app/robots.txt = Allow: /; live home <link rel=canonical href="https://physiodanali.gr"> with no robots meta; live og:image=https://physiodanali.gr/opengraph-image
- **Fix:** Serve noindex ONLY on the non-canonical vercel.app host until DNS cutover (e.g. env/host check in robots.ts or an X-Robots-Tag:noindex when Host != physiodanali.gr), then flip DNS and drop the noindex in the same change window. Before cutover, cross-check every currently-indexed old URL (GSC Coverage export) against src/lib/legacy-redirects.ts so nothing 404s at flip.
- **Verify (CONFIRMED):** Every claim reproduced against the live production deployment and the repo. (1) src/lib/seo.ts:3 hardcodes SITE_URL="https://physiodanali.gr" and everything derives from it. (2) Live https://physiodanali.vercel.app/robots.txt returns `User-Agent: * / Allow: /` (only /api/ and /admin/ disallowed) — the host is fully crawlable. (3) `curl -I` on the vercel home shows NO X-Robots-Tag header (Vercel on

### 🟠 HIGH · on-page-seo · ⚠️SEO — All area-lander <title> tags double-print the brand ("… — PhysioDanali · PhysioDanali")
- **Πρόβλημα:** Every area-lander meta.title already ends with "— PhysioDanali", and layout's title template appends " · PhysioDanali" on top, producing e.g. live title "Φυσικοθεραπεία κατ' οίκον στη Γλυφάδα — PhysioDanali · PhysioDanali". This affects all 7 EL + EN area landers (physiotherapy-{glyfada,voula,vari,vouliagmeni} + chiropractic-{glyfada,vari,vouliagmeni}). Wastes SERP title pixels, risks truncation of the locality keyword, and reads as an error — a CTR drag on exactly the local-intent queries the old site ranks for.
- **Απόδειξη:** src/lib/area-landers.ts:51,88,125,162,199,236,273,311,345,379 titles end "— PhysioDanali"; src/app/physiotherapy-glyfada/page.tsx:11 returns data.meta.title; layout template "%s · PhysioDanali"; live curl https://physiodanali.vercel.app/physiotherapy-glyfada <title>…στη Γλυφάδα — PhysioDanali · PhysioDanali</title>
- **Fix:** Either strip "— PhysioDanali" from the meta.title strings in area-landers.ts, or return { title: { absolute: data.meta.title } } from generateMetadata so the layout template does not re-append the brand.
- **Verify (CONFIRMED):** Independently reproduced. src/lib/area-landers.ts meta.title strings end in "— PhysioDanali" (e.g. "Φυσικοθεραπεία κατ' οίκον στη Γλυφάδα — PhysioDanali"). src/app/physiotherapy-glyfada/page.tsx:11 returns { title: data.meta.title } (plain string, not {absolute}), so layout's title.template "%s · PhysioDanali" (src/app/layout.tsx:55) re-appends the brand. Live curl of https://physiodanali.vercel.a

### 🟠 HIGH · technical-seo · ⚠️SEO — aggregateRating is self-serving (no Review objects) and understated (100 vs real 122)
- **Πρόβλημα:** localBusinessSchema emits ratingValue 5.0 / reviewCount 100 with no nested `review` array. Google does not show review stars for self-serving LocalBusiness aggregateRating that lacks associated Review markup, so this is unlikely to earn (and may forfeit) star rich results, and reviewCount understates the real Google profile (122). The rating block is injected globally, so it also appears on articles/contact/etc. where it is even less appropriate.
- **Απόδειξη:** src/lib/seo.ts:58-64 (ratingValue "5.0", reviewCount "100", no review[]); recon: real Google profile = 122 reviews; live /low-back-pain and /contact both emit the AggregateRating block
- **Fix:** Set reviewCount to the true 122 and add a small set of nested Review objects (author, reviewRating, reviewBody) to make the aggregateRating rich-result eligible, or remove aggregateRating from pages where it is self-serving to avoid a policy violation.
- **Verify (CONFIRMED):** Reproduced all three sub-claims. (1) src/lib/seo.ts:58-64 emits aggregateRating with ratingValue "5.0", reviewCount "100", bestRating/worstRating, and NO nested `review[]` array — verified by reading the file. (2) It is injected globally: src/app/layout.tsx:104 renders `<JsonLd data={[localBusinessSchema, personSchema]} />` in the ROOT layout, so the block appears on every page. Live curl confirms

### 🟠 HIGH · content-parity · ⚠️SEO — 15 rebuilt articles exist but old blog 301s point to /articles list or hubs, not the matching article
- **Πρόβλημα:** 18 content-rich GR articles (4,000–6,500 words, unique titles, indexable, self-canonical) are live at /articles/<slug>, but the legacy redirect map sends their equivalent OLD blog URLs to the generic /articles list or a service hub instead of the specific article. Confirmed live 308s: /el/blog-disc-herniation-gr.html→/articles (article /articles/disc-herniation exists), blog-frozen-shoulder→/articles, blog-rotator-cuff→/articles, blog-vestibular-rehab→/articles, blog-multiple-sclerosis→/articles, blog-parkinsons-exercise→/articles, blog-tendinopathy-or-tendonitis→/articles, blog-elbow/foot/hand-pain→/therapies, blog-sprained-ankle→/therapies, blog-knee-pain→/therapies, blog-shoulder-pain→/therapies, blog-brazilian-drainage→/lymphatic, blog-stroke-rehab + blog-stroke-rehabilitation→/articles. Root cause: article slugs shipped differently than the plan's Appendix A targets (built 'tendinopathy' not 'tendinopathy-vs-tendonitis'; 'brazilian-drainage' not 'brazilian-lymphatic-drainage'; one 'stroke-rehabilitation' not two), so the map was never repointed. Redirecting a ranking blog post to a list page is a soft-consolidation that dilutes its accrued equity and drops the article from directly inheriting the old URL's rankings.
- **Απόδειξη:** src/lib/legacy-redirects.ts:100-127 (targets /articles,/therapies,/lymphatic); live: /el/blog-disc-herniation-gr.html 308→/articles while /articles/disc-herniation returns 200 with 6,306-word body
- **Fix:** Repoint these rows in src/lib/legacy-redirects.ts to the actual published slugs: blog-disc-herniation→/articles/disc-herniation, blog-frozen-shoulder→/articles/frozen-shoulder, blog-rotator-cuff→/articles/rotator-cuff, blog-vestibular-rehab→/articles/vestibular-rehab, blog-multiple-sclerosis→/articles/multiple-sclerosis, blog-parkinsons-exercise→/articles/parkinsons-exercise, blog-tendinopathy-or-tendonitis→/articles/tendinopathy, blog-elbow-pain→/articles/elbow-pain, blog-foot-pain→/articles/foot-pain, blog-hand-pain→/articles/hand-pain, blog-sprained-ankle-treatment→/articles/sprained-ankle, blog-knee-pain→/articles/knee-pain, blog-shoulder-pain→/articles/shoulder-pain, blog-brazilian-drainage→/articles/brazilian-drainage, blog-stroke-rehab + blog-stroke-rehabilitation→/articles/stroke-rehabilitation. Cross-check the full published slug list (curl /articles) against every blog-* row before cutover.
- **Verify (CONFIRMED):** Independently reproduced both sides. src/lib/legacy-redirects.ts:100-127 points the GR blog URLs at generic /articles, /therapies, or /lymphatic. Live checks on physiodanali.vercel.app: blog-disc-herniation-gr, -frozen-shoulder, -rotator-cuff, -vestibular-rehab, -tendinopathy-or-tendonitis, -stroke-rehab, -stroke-rehabilitation, -multiple-sclerosis, -parkinsons-exercise all 308→/articles; blog-elb

### 🟠 HIGH · content-parity · ⚠️SEO — Shoulder-pain and knee-pain head-terms: old treatment pages + sub-clusters all 301 to generic /therapies despite rich articles existing
- **Πρόβλημα:** Two of the highest-volume condition head-terms have no dedicated page. The old high-value pages /el/shoulder-pain-treatment-gr.html and /el/knee-pain-treatment-gr.html (verified live 308→/therapies) plus their entire legacy sub-clusters (frozen-shoulder, shoulder-impingement, shoulder-arthritis, calcific-tendonitis, supraspinatus, rotator-cuff; knee-osteoarthritis, meniscus, ACL, MCL-LCL, patellofemoral, ITB — legacy-redirects.ts:186,189,192-193,196,211-223) all funnel to the generic /therapies hub. Meanwhile /articles/shoulder-pain (4,382 words) and /articles/knee-pain are live and would be far more topically relevant 301 targets. The plan (Appendix A) explicitly wanted P1 /shoulder-pain and /knee-pain condition pages. Sending a ranking 'θεραπεία πόνου ώμου/γόνατος' URL to a broad services page loses that specific ranking.
- **Απόδειξη:** live /el/shoulder-pain-treatment-gr.html & /el/knee-pain-treatment-gr.html 308→/therapies; docs/seo-migration-plan.md Appendix A P1 /shoulder-pain,/knee-pain; /articles/shoulder-pain 200 (4,382 words)
- **Fix:** Minimum: repoint shoulder-pain-treatment and knee-pain-treatment (and their sub-cluster rows) from /therapies to /articles/shoulder-pain and /articles/knee-pain. Better: build dedicated /shoulder-pain and /knee-pain condition pages using the existing condition-page-template (FAQPage+MedicalTherapy schema) as landing targets for these clusters, since these are commercial head-terms not just informational blog topics.
- **Verify (CONFIRMED):** Fully reproduced. Live curl: /el/shoulder-pain-treatment-gr.html and /el/knee-pain-treatment-gr.html both 308→/therapies. legacy-redirects.ts confirms both head-terms (GR+EN, lines 186/189/192-193) plus their entire sub-clusters (knee: osteoarthritis L212, meniscus, ACL, MCL-LCL, patellofemoral; shoulder: frozen L183, arthritis L218, calcific L219, impingement L220, supraspinatus, rotator-cuff; pl

### 🟠 HIGH · content-parity · ⚠️SEO — Flagship neuro-rehab cluster never built and its old URLs 301 to two different hubs (split equity)
- **Πρόβλημα:** The plan lists /neuro-rehab as a P0 flagship rebuild consolidating an 8+ URL neuro cluster (per-area neuro landers, neurological-physical-therapy, plus stroke/MS/cerebral-palsy/Parkinson content). It was not built (no src/app/neuro-rehab). Worse, the surviving cluster is split across hubs: /el/physical-therapy-neuro-at-home-gr.html 308→/home-care but /el/neurological-physical-therapy-gr.html 308→/therapies — the same neuro topic pointed at two unrelated hubs, so no single page accumulates the cluster's authority. The stroke-rehabilitation, multiple-sclerosis and parkinsons-exercise articles are live but the neuro-cluster URLs don't point to them either. This is the brand's post-stroke/neurological at-home service — a real ranking asset being scattered.
- **Απόδειξη:** no src/app/neuro-rehab dir; live /el/physical-therapy-neuro-at-home-gr.html→/home-care vs /el/neurological-physical-therapy-gr.html→/therapies; docs/seo-migration-plan.md Appendix A P0 /neuro-rehab (+5 URLs)
- **Fix:** Build a /neuro-rehab hub (P0 per plan) and repoint the whole neuro cluster to it consistently; interlink the stroke/MS/Parkinson/cerebral-palsy articles from it. At minimum, make all neuro-* legacy rows target one destination instead of splitting between /home-care and /therapies.
- **Verify (CONFIRMED):** Every factual claim reproduced. (1) No src/app/neuro-rehab dir exists (confirmed by ls of src/app — the neuro-rehab hub was never built). (2) In src/lib/legacy-redirects.ts the neuro cluster is genuinely split: line 55 /el/physical-therapy-neuro-at-home-gr.html → /home-care, line 62 /el/neurological-physical-therapy-gr.html → /therapies. (3) Live 308s on physiodanali.vercel.app confirm the split e

### 🟠 HIGH · local-geo · ⚠️SEO — Alimos and Nea Smyrni area landers (physio + chiro) never built; local head-terms 301 to generic hubs
- **Πρόβλημα:** The plan lists P0 /physiotherapy-alimos, P0 /physiotherapy-nea-smyrni, P1 /chiropractic-alimos and P1 /chiropractic-nea-smyrni. None exist (verified no src/app dirs). The old area URLs (/el/physical-therapy-at-home-alimos-gr.html, ...-neasmyrni, /el/chiropractor-athome-alimos-gr.html, ...-neasmyrni) 301 to generic /home-care and /chiropractic (legacy-redirects.ts:21,23,29,30). Area landers are exactly the pages that rank for 'φυσικοθεραπεία κατ' οίκον Άλιμος' / 'χειροπρακτική Νέα Σμύρνη' local-pack queries, and dumping them into a generic hub loses that geo-specific ranking. Note Glyfada/Voula/Vari/Vouliagmeni DID get dedicated landers (area-landers.ts), so Alimos/Nea Smyrni are the two gaps. Also the LocalBusiness areaServed lists Alimos but omits Nea Smyrni entirely.
- **Απόδειξη:** no src/app/physiotherapy-alimos|nea-smyrni|chiropractic-alimos|nea-smyrni; legacy-redirects.ts:21,23,29,30→/home-care,/chiropractic; docs/seo-migration-plan.md Appendix A P0/P1
- **Fix:** If Alimos and Nea Smyrni are still served (open question #6 in the plan), build the four landers via the existing area-landers.ts pattern and repoint those legacy rows to them; add Nea Smyrni to areaServed in src/lib/seo.ts. If not served, leave the hub 301s but confirm explicitly so the loss is intentional.
- **Verify (CONFIRMED):** Independently reproduced every claim. (1) No landers exist: src/app has physiotherapy-{glyfada,voula,vari,vouliagmeni} and chiropractic-{glyfada,vari,vouliagmeni} only — no physiotherapy-alimos, physiotherapy-nea-smyrni, chiropractic-alimos, or chiropractic-nea-smyrni dirs, and area-landers.ts keys confirm the same 7 slugs (no alimos/smyrni). (2) legacy-redirects.ts lines 21,23,29,30 are exactly a

### 🟠 HIGH · local-geo · ⚠️SEO — Opening hours conflict: llms.txt + area-lander FAQ schema say 10:00–22:00 while GBP, LocalBusiness schema and the whole site UI say 08:00–23:00
- **Πρόβλημα:** Two contradictory hour sets are published to Google and to LLMs. Canonical/correct hours are 08:00–23:00: LocalBusiness openingHoursSpecification (seo.ts:49-50), every UI surface (content.ts:11-14 hoursShort/hoursList, plus ~15 marketing strings all saying 'έως 23:00'), and the Google Business Profile (open until 23:00) all agree on 08:00–23:00. But public/llms.txt states '10:00–22:00' in three places (header, English intro, Booking 'Ωράριο'), and area-landers.ts:29-43 FAQ_HOURS_EL/FAQ_HOURS_EN hardcode '10:00 έως 22:00' / '10:00 to 22:00'. Those FAQ answers are rendered as user-facing copy AND emitted inside FAQPage JSON-LD on all 7 area landing pages (verified live: physiodanali.vercel.app/llms.txt returns 10:00–22:00). Wrong hours in FAQPage structured data can surface in SERP rich results, and llms.txt is the primary hours source answer-engines (ChatGPT/Perplexity/Claude, which robots.ts explicitly welcomes) will quote — so LLMs will tell patients the practice closes at 22:00 and opens at 10:00, contradicting the real GBP hours.
- **Απόδειξη:** public/llms.txt lines 'καθημερινά 10:00–22:00', 'Daily 10:00–22:00', 'Ωράριο: Δευτέρα–Κυριακή 10:00–22:00'; src/lib/area-landers.ts:31-32 & 41-42 FAQ_HOURS '10:00 έως 22:00' / '10:00 to 22:00'; vs src/lib/seo.ts:49-50 opens 08:00/closes 23:00 and src/lib/content.ts:11-14 '08:00 — 23:00'
- **Fix:** Change all hour references in public/llms.txt and the FAQ_HOURS_EL/FAQ_HOURS_EN constants in src/lib/area-landers.ts to 08:00–23:00 so llms.txt, the area-page FAQPage schema, the sitewide LocalBusiness schema, the UI and GBP all state identical hours before cutover.
- **Verify (CONFIRMED):** Independently reproduced every cited fact. public/llms.txt states 10:00–22:00 in 3 places (lines 6, 10, 31) — verified identical on live physiodanali.vercel.app/llms.txt. src/lib/area-landers.ts FAQ_HOURS_EL (line 32) and FAQ_HOURS_EN (line 42) hardcode '10:00 έως 22:00' / '10:00 to 22:00', and these constants feed FAQPage JSON-LD across all 7 area landers (referenced at lines 80,117,154,191,228,2

### 🟠 HIGH · local-geo · ⚠️SEO — aggregateRating reviewCount is understated and internally inconsistent (schema 100 vs reviews page 121 vs GBP 122)
- **Πρόβλημα:** The sitewide LocalBusiness aggregateRating hardcodes reviewCount '100' (verified live on physiodanali.vercel.app). The /reviews page (metadata, hero lead and the star line) says '121 reviews'. The real Google Business Profile has 122. Three different numbers for the same business is a consistency signal problem, and 100 understates the actual, stronger social proof by 22 reviews. Under-reporting weakens the review-count rich signal and risks a mismatch flag between on-page schema and Google's own known review total.
- **Απόδειξη:** src/lib/seo.ts:61 reviewCount:"100" (live: "aggregateRating":{..."reviewCount":"100"}); src/app/reviews/page.tsx:53,58,84-85,107 '121 reviews' / '121 αξιολογήσεις'; GBP actual 122
- **Fix:** Set reviewCount to the real current Google total (122) in seo.ts and update the /reviews page copy to the same number; keep a single source of truth so the count only needs updating in one place.
- **Verify (CONFIRMED):** Independently reproduced. src/lib/seo.ts:61 hardcodes reviewCount:"100" and the live physiodanali.vercel.app JSON-LD serves exactly "aggregateRating":{"@type":"AggregateRating","ratingValue":"5.0","reviewCount":"100","bestRating":"5","worstRating":"1"}. The /reviews page independently states 121 in five places: metadata descriptions (page.tsx:53,58), hero lead (84-85), and the star line (107) — "1

### 🟠 HIGH · local-geo · ⚠️SEO — Alimos is marketed as a served city but has NO dedicated landing page — local coverage regression vs the old site
- **Πρόβλημα:** Άλιμος is listed in LocalBusiness areaServed (seo.ts:36), promoted in llms.txt and in the homepage hero/areas grid (content.ts:238 'Άλιμος'), yet there is no physiotherapy-alimos or chiropractic-alimos area lander — /physiotherapy-alimos returns 404 (verified live). The old WordPress site ranked with dedicated Alimos pages (/el/physical-therapy-at-home-alimos-gr.html is live 200 today, plus chiropractor-athome-alimos and physical-therapy-neuro-at-home-alimos). Those legacy URLs are 301'd to the GENERIC /home-care and /chiropractic (legacy-redirects.ts:21,29,44), which preserves link equity but drops the Alimos-specific keyword targeting, so the site will likely lose ranking for 'φυσικοθεραπεία Άλιμος' / 'χειροπρακτική Άλιμος'. The other 4 served cities (Glyfada/Voula/Vari/Vouliagmeni) each have a bespoke lander; Alimos is the sole gap.
- **Απόδειξη:** live: physiodanali.vercel.app/physiotherapy-alimos => 404, old physiodanali.gr/el/physical-therapy-at-home-alimos-gr.html => 200; src/lib/legacy-redirects.ts:21 alimos->/home-care, :29 chiro alimos->/chiropractic; src/lib/seo.ts:36 areaServed includes Άλιμος; no 'alimos' key in src/lib/area-landers.ts
- **Fix:** Create physiotherapy-alimos (and chiropractic-alimos) area landers with unique Alimos copy matching the existing pattern, then repoint the alimos legacy redirects to those pages instead of the generic /home-care and /chiropractic.
- **Verify (CONFIRMED):** Independently reproduced every claim. Live: physiodanali.vercel.app/physiotherapy-alimos = 404 and /chiropractic-alimos = 404; old physiodanali.gr/el/physical-therapy-at-home-alimos-gr.html = 200. area-landers.ts has NO 'alimos' key (grep confirms) — physiotherapy landers exist only for glyfada/vari/voula/vouliagmeni, so Alimos is the sole served city with zero landers. Meanwhile Alimos IS markete

### 🟠 HIGH · functionality — Contact form is a mailto: hack with no backend — leads silently lost
- **Πρόβλημα:** src/components/site/contact-form.tsx onSubmit does `window.location.href = mailto:info@physiodanali.gr?...` (comment on line 34: "No backend yet — open mail client"). It then fakes success after a 600ms timeout regardless of whether an email was actually sent. On mobile browsers, webmail-only users, or desktops with no default mail client configured, clicking Submit opens nothing (or a broken handler) yet the UI shows the green success state. The form collects a GDPR consent checkbox and links the privacy policy, implying server-side processing that never happens. Every such lead is lost with the user believing it was received. Booking flow, by contrast, is a real Supabase+server-action backend (src/app/booking/actions.ts) — the contact form is the odd one out.
- **Απόδειξη:** src/components/site/contact-form.tsx:34-42 (mailto + setTimeout success); contrast src/app/booking/actions.ts:41 submitBookingAction (real backend)
- **Fix:** Replace the mailto with a real server action that inserts to Supabase and sends via Resend (reuse the pattern in src/app/booking/actions.ts). Only show the success state after the action resolves ok; show an error otherwise. Add the honeypot/Turnstile already used on booking.
- **Verify (CONFIRMED):** Reproduced directly in src/components/site/contact-form.tsx. onSubmit (lines 30-41): line 34 comment "No backend yet — open mail client", line 39 sets window.location.href to a mailto: link (mailto:${site.email}?subject=...&body=...), and line 40 fires setTimeout(() => setStatus("success"), 600) unconditionally. The success branch (lines 43-68) then renders the green Check + "success" title regard

### 🟠 HIGH · analytics · ⚠️SEO — Zero analytics installed — cannot detect traffic/ranking loss at cutover
- **Πρόβλημα:** grep across src/ and public/ for gtag/googletagmanager/plausible/posthog/GA/umami/matomo returns nothing; package.json has no analytics dependency. There is no GA4, no Plausible, no Vercel Analytics. The #1 stated goal is being 1000% sure no SEO is lost at cutover — but with no analytics and (finding below) no Search Console verification, there is no instrument to observe a traffic drop after DNS flips from the old WordPress site. You would be flying blind precisely when monitoring matters most.
- **Απόδειξη:** grep -rniE 'gtag|googletagmanager|plausible|posthog|analytics|umami|matomo' src/ public/ → no analytics library; package.json dependencies list has none
- **Fix:** Install a privacy-friendly analytics before cutover (Plausible/Umami need no consent banner; or GA4 gated behind a consent banner). Also add Vercel Web Analytics for RUM. Baseline old-site traffic in GSC/GA now so you can compare post-cutover.
- **Verify (CONFIRMED):** Independently reproduced. (1) package.json dependencies contain zero analytics libs — no @vercel/analytics, no gtag, no plausible/posthog/umami/matomo. (2) grep across src/ for next/script, @vercel/analytics, SpeedInsights, gtag, GoogleTag, Insights returns nothing (the only grep hits are Tailwind class names like bg-porcelain/bg-gradient and the word 'description' in metadata — false positives). 

### 🟠 HIGH · seo-migration · ⚠️SEO — GOOGLE_SITE_VERIFICATION not present in live HTML — GSC can't verify at cutover
- **Πρόβλημα:** layout.tsx gates the verification meta tag on the GOOGLE_SITE_VERIFICATION env var, which is only in .env.example (blank) and NOT in .env.local. Fetching https://physiodanali.vercel.app/ shows no google-site-verification meta tag in <head>. Without Search Console verification you cannot submit the new sitemap, submit sitemap-legacy.xml to force recrawl of the 264 redirects, use the Change of Address tool, or monitor Coverage/traffic during migration — all core to a no-SEO-loss cutover.
- **Απόδειξη:** curl https://physiodanali.vercel.app/ | grep google-site-verification → empty; .env.example line has blank GOOGLE_SITE_VERIFICATION; not in .env.local key list
- **Fix:** Add GOOGLE_SITE_VERIFICATION to Vercel production env (and verify via DNS TXT as a second method) before cutover. Confirm the tag renders live, then verify the property in GSC and submit both sitemaps.
- **Verify (CONFIRMED):** Reproduced all evidence. curl of https://physiodanali.vercel.app/ returns no google-site-verification meta tag (grep exit 1). layout.tsx lines 61-63 gate metadata.verification on process.env.GOOGLE_SITE_VERIFICATION and emit undefined when unset. .env.local has 0 occurrences of the key; .env.example:19 defines it blank/optional. So on the current Vercel deploy the HTML-tag verification is genuinel

### 🟠 HIGH · functionality — Patient/booking emails send from agency domain amox.gr; physiodanali.gr not set up in Resend
- **Πρόβλημα:** All booking notification/confirmation emails default from `PhysioDanali <noreply@amox.gr>` (src/app/booking/actions.ts:156, src/lib/booking.ts:365/407/519) and notify.ts defaults to onboarding@resend.dev. DNS checks: amox.gr HAS a Resend DKIM key (resend._domainkey.amox.gr resolves) and DMARC p=none, so amox.gr is the only Resend-verified sender — that is why it's the default. But (a) patients who book "PhysioDanali" receive confirmations from an unrelated agency domain (amox.gr), which looks like phishing and hurts deliverability/trust; (b) physiodanali.gr has NO Resend DKIM and NO DMARC record at all, so if anyone sets NOTIFY_FROM to a @physiodanali.gr address to fix the branding, every booking email will fail SPF/DKIM alignment and land in spam or bounce. Additionally, RESEND_API_KEY must be present in Vercel prod or ALL notify/confirm functions silently no-op (if (!key) return) — meaning bookings would be created with the owner never emailed.
- **Απόδειξη:** src/app/booking/actions.ts:156 from default noreply@amox.gr; dig resend._domainkey.physiodanali.gr → empty, dig _dmarc.physiodanali.gr → empty; dig resend._domainkey.amox.gr → DKIM present; src/lib/booking.ts:363 `if (!key) return` silent no-op
- **Fix:** Verify physiodanali.gr as a sending domain in Resend (add its DKIM CNAMEs + a return-path), add a DMARC record for physiodanali.gr, then set NOTIFY_FROM=PhysioDanali <noreply@physiodanali.gr> in Vercel. Confirm RESEND_API_KEY, NOTIFY_TO and BOOKING_NOTIFY_TO are set in Vercel prod and send a live test booking end-to-end.
- **Verify (CONFIRMED):** Every claim independently reproduced. Code: noreply@amox.gr is the NOTIFY_FROM default at actions.ts:157, booking.ts:365, booking.ts:406, and booking.ts:527 (finding cited 519 — minor line drift, substance correct); notify.ts:17 defaults to onboarding@resend.dev; all send paths silently no-op when RESEND_API_KEY is unset (actions.ts:155, booking.ts:363/404, notify.ts). DNS confirms and strengthens

### 🟡 MEDIUM · technical-seo · ⚠️SEO — physiodanali.vercel.app is fully crawlable/indexable (200 to Googlebot, robots Allow, no noindex) — duplicate of the future .gr
- **Πρόβλημα:** The staging/production Vercel domain serves the full site to Googlebot with HTTP 200, no X-Robots-Tag, no <meta name=robots noindex>, and robots.txt Allow: /. Its pages canonicalize to physiodanali.gr, which mitigates indexing, but pre-cutover Google can crawl and potentially index vercel.app (its canonical target currently serves DIFFERENT content, so Google may not trust the canonical and could index the vercel.app URL as its own). Post-cutover the vercel.app keeps serving identical content.
- **Απόδειξη:** curl -A Googlebot https://physiodanali.vercel.app/ -> HTTP/2 200, no x-robots-tag; robots.txt Allow: /. Canonical -> physiodanali.gr (which serves old site now).
- **Fix:** Gate indexing on the non-production host: emit X-Robots-Tag: noindex (or robots noindex) when the request Host is *.vercel.app, or enable Vercel deployment protection on preview while leaving the production custom domain open. Ensure Googlebot is NOT blocked on physiodanali.gr after cutover. This removes the duplicate-content ambiguity.
- **Verify (CONFIRMED):** Reproduced every claim. curl with Googlebot UA to https://physiodanali.vercel.app/ returns HTTP/2 200 with NO x-robots-tag header and no cache-control noindex signal; response body has no <meta name="robots"> noindex tag at all. robots.txt served from the vercel.app host is fully permissive (User-Agent: * / Allow: / with only /api/ and /admin/ disallowed) — so the entire public site is crawlable o

### 🟡 MEDIUM · seo-migration — New robots.txt reverses the owner's deliberate AI-crawler policy (opens training bots the old site blocked; uses deprecated Claude-Web)
- **Πρόβλημα:** The OLD robots.txt intentionally ALLOWED answer/search bots (OAI-SearchBot, ChatGPT-User, PerplexityBot, ClaudeBot, Claude-User, Claude-SearchBot) and DISALLOWED training-only bots (GPTBot, Google-Extended, CCBot, Bytespider). The NEW robots.ts explicitly ALLOWS GPTBot and Google-Extended (training crawlers the owner blocked) and does not block CCBot/Bytespider (so the '*' Allow:/ lets them in). It also lists the deprecated 'Claude-Web' instead of the current 'Claude-User' and 'Claude-SearchBot'. This silently opts the practice's content back into AI-training corpora against documented intent. Not a search-ranking loss, but a policy regression the owner explicitly configured.
- **Απόδειξη:** Old: curl https://physiodanali.gr/robots.txt disallows GPTBot/Google-Extended/CCBot/Bytespider, allows Claude-User/Claude-SearchBot. New: src/app/robots.ts:7-15 AI_CRAWLERS includes GPTBot + Google-Extended as allow, uses 'Claude-Web', no CCBot/Bytespider disallow.
- **Fix:** Mirror the owner's original stance in robots.ts: Disallow GPTBot, Google-Extended, CCBot, Bytespider; Allow OAI-SearchBot, ChatGPT-User, PerplexityBot, ClaudeBot, Claude-User, Claude-SearchBot. Confirm with the owner before changing, since it's a deliberate GEO/data-rights choice.

### 🟡 MEDIUM · local-geo · ⚠️SEO — GBP website will resolve to the new homepage at cutover — verify NAP + hours consistency (schema 08:00-23:00 vs llms.txt 10:00-22:00)
- **Πρόβλημα:** The Google Business Profile (5.0 / 122 reviews) links to physiodanali.gr, which becomes the new homepage at cutover. Local-pack ranking depends on exact NAP + hours consistency between GBP and on-page/schema data. The new LocalBusiness schema uses hours 08:00-23:00 (matches the old site, good) and the correct NAP/phone, but llms.txt advertises 10:00-22:00 — a conflicting hours signal that also reaches answer engines. Any NAP/hours drift after the GBP starts pointing at the new page weakens local consistency.
- **Απόδειξη:** seo.ts openingHoursSpecification 08:00-23:00 matches old site (RECON D: 08:00-23:00 all 7 days). llms.txt states 10:00-22:00 (RECON A). GBP 122 reviews vs schema aggregateRating reviewCount 100 (seo.ts:60-61).
- **Fix:** Pick ONE canonical hours set and make schema, llms.txt, layout copy, and GBP all agree. Confirm the new homepage's LocalBusiness NAP (Αγίου Νεκταρίου 58, 16562 Γλυφάδα, +306944344342) byte-matches GBP. Optionally align aggregateRating reviewCount to the real 122 (and add Review objects) so the stronger social proof is reflected and star rich-results remain eligible.
- **Verify (CONFIRMED):** Independently reproduced. src/lib/seo.ts sets openingHoursSpecification opens 08:00 / closes 23:00 and aggregateRating.reviewCount "100" with NAP Αγίου Νεκταρίου 58, 16562 Γλυφάδα, phone site.phone. public/llms.txt contradicts this with 10:00–22:00 in three spots (Greek intro, English line, and Booking "Δευτέρα–Κυριακή 10:00–22:00"). Live homepage https://physiodanali.vercel.app/ ships the schema 

### 🟡 MEDIUM · local-geo · ⚠️SEO — Opening hours conflict: JSON-LD says 08:00–23:00 but public/llms.txt says 10:00–22:00
- **Πρόβλημα:** The LocalBusiness schema openingHoursSpecification is 08:00–23:00 (matching the old site and the homepage description 'έως τις 23:00'), but public/llms.txt advertises 10:00–22:00. LLM answer engines (which robots.ts deliberately welcomes) will read the conflicting llms.txt hours, and inconsistent NAP/hours signals weaken local-pack trust.
- **Απόδειξη:** src/lib/seo.ts:49-50 opens 08:00 / closes 23:00; public/llms.txt hours 10:00-22:00; live home meta description '…Έως τις 23:00'
- **Fix:** Pick one canonical hours set (old site + GBP = 08:00–23:00) and correct public/llms.txt to match the schema.
- **Verify (CONFIRMED):** Reproduced all three claims. src/lib/seo.ts:49-50 has opens "08:00" / closes "23:00" in the LocalBusiness openingHoursSpecification, and the live site's rendered JSON-LD confirms "opens":"08:00","closes":"23:00". public/llms.txt contradicts this in three places: line 6 (καθημερινά 10:00–22:00), line 10 (Daily 10:00–22:00 (Europe/Athens)), and line 31 (Ωράριο: Δευτέρα–Κυριακή 10:00–22:00). The live

### 🟡 MEDIUM · seo-migration — GOOGLE_SITE_VERIFICATION not set in Vercel prod — verification meta is absent live
- **Πρόβλημα:** The verification meta tag is env-gated and is ABSENT from the live production HTML, meaning GOOGLE_SITE_VERIFICATION is not populated in Vercel. If GSC ownership at cutover relies on the HTML-tag method, verification (and thus sitemap submission / migration monitoring) will fail until it is set.
- **Απόδειξη:** src/app/layout.tsx:61 verification gated on env; live curl home shows no <meta name="google-site-verification"> (ABSENT); only present in .env.example
- **Fix:** Set GOOGLE_SITE_VERIFICATION in Vercel production env before cutover (or verify GSC via DNS TXT so it is domain-scoped and survives the DNS flip).

### 🟡 MEDIUM · on-page-seo · ⚠️SEO — Homepage <title> dropped the city + '(έως 23:00)' hooks the old site ranked with
- **Πρόβλημα:** New homepage title is brand-led 'PhysioDanali — Χειροπρακτική και Φυσικοθεραπεία κατ' οίκον' with no city and no availability hook. The old homepage title was locality + availability + brand-last: 'PhysioDanali | Χειροπρακτική & Φυσικοθεραπεία κατ' οίκον στη Βούλα (έως 23:00)'. Dropping 'Βούλα/Γλυφάδα' and '(έως 23:00)' from the highest-authority page's title can cost CTR on the exact local queries currently ranking (the meta description still carries locality, but titles weigh more for CTR). Interior area titles do keep locality, so this is specifically the homepage.
- **Απόδειξη:** live home <title>PhysioDanali — Χειροπρακτική και Φυσικοθεραπεία κατ' οίκον</title>; recon D old homepage title included 'στη Βούλα (έως 23:00)'
- **Fix:** Reintroduce a locality + availability hook into the homepage title (e.g. '… κατ' οίκον στη Γλυφάδα & Βούλα — έως 23:00') while keeping the brand, matching the old CTR-tested pattern.
- **Verify (CONFIRMED):** All evidence reproduced independently. New live homepage title (physiodanali.vercel.app): "PhysioDanali — Χειροπρακτική και Φυσικοθεραπεία κατ' οίκον" — no city, no "(έως 23:00)" hook. Old live homepage title (physiodanali.gr): "PhysioDanali | Χειροπρακτική & Φυσικοθεραπεία κατ' οίκον στη Βούλα (έως 23:00)" — carries both the locality (Βούλα) and availability hook. Source confirmed in src/app/layo

### 🟡 MEDIUM · technical-seo — about and contact pages lack BreadcrumbList; contact lacks ContactPoint schema
- **Πρόβλημα:** Service/condition/area pages emit MedicalTherapy + FAQPage + BreadcrumbList, but /about and /contact carry only the globally-injected business+person blocks — no page-level BreadcrumbList, and /contact has no ContactPoint. Verified live: /contact JSON-LD contains only City/GeoCoordinates/Person/PostalAddress/OpeningHours/AggregateRating (the global block), no BreadcrumbList or ContactPoint. Structured-data completeness gap, not a ranking loss.
- **Απόδειξη:** live curl https://physiodanali.vercel.app/contact JSON-LD @type set has no BreadcrumbList/ContactPoint; recon confirms about/contact have no page-level JsonLd
- **Fix:** Add breadcrumbSchema to /about and /contact, and a ContactPoint (telephone, contactType, availableLanguage, areaServed) on /contact.

### 🟡 MEDIUM · content-parity · ⚠️SEO — ~30 English blog posts all 301 to /en/therapies — entire EN long-tail abandoned
- **Πρόβλημα:** The old site had ~30 EN blog posts (recon D) that ranked for English-language condition queries (expat/tourist audience in Glyfada/Vouliagmeni). The new blog is Greek-only, so every EN blog URL 301s to a hub: verified /en/blog-disc-herniation-eng.html, /en/blog-frozen-shoulder-eng.html, /en/blog-vestibular-rehab-eng.html all 308→/en/therapies (legacy-redirects.ts:128-155). No EN equivalent content exists, so these rankings are lost outright rather than migrated. Given the affluent expat market this site explicitly targets, EN informational long-tail may have real value.
- **Απόδειξη:** live /en/blog-*-eng.html 308→/en/therapies; legacy-redirects.ts:128-155; recon D ~30 EN blog posts
- **Fix:** Decide deliberately (plan open question #10): either translate the top EN articles (the AI CMS supports it) and repoint the EN blog 301s to them, or accept the loss and confirm the EN blog audience is negligible from GSC before cutover. At minimum repoint EN blog URLs that have a Greek article to the /en version of the most relevant existing hub rather than the generic /en/therapies.
- **Verify (CONFIRMED):** Independently reproduced. Live: /en/blog-disc-herniation-eng.html, /en/blog-frozen-shoulder-eng.html, /en/blog-vestibular-rehab-eng.html, plus /en/blog-carpal-tunnel-eng.html and /en/blog-rotator-cuff-eng.html all return 308 -> https://physiodanali.vercel.app/en/therapies. legacy-redirects.ts contains 28 /en/blog-* entries (finding says ~30, matching recon D's estimate — minor overstatement); 23 o

### 🟡 MEDIUM · local-geo · ⚠️SEO — /lymphatic-glyfada niche geo-lander not built; area lymphatic URLs collapse to generic /lymphatic
- **Πρόβλημα:** The plan flags /lymphatic-glyfada as P1 — a high-margin post-liposuction niche recently invested in with fresh copy, Glyfada being the cosmetic-surgery clientele hotspot. It wasn't built; /el/intensive-lymphatic-drainage-glyfada-gr.html and the Vari variant 301 to the generic /lymphatic hub (legacy-redirects.ts:31,43,52). Loses the geo-qualified 'λεμφικό μασάζ / drainage Γλυφάδα' term that converts for the cosmetic-surgery audience. Lower search volume than the condition head-terms, hence medium.
- **Απόδειξη:** legacy-redirects.ts:31,43,52→/lymphatic; docs/seo-migration-plan.md Appendix A P1 /lymphatic-glyfada
- **Fix:** If the post-lipo lymphatic service is actively marketed, build /lymphatic-glyfada (area-landers pattern) and repoint the Glyfada lymphatic rows to it; otherwise the /lymphatic consolidation is acceptable — confirm with owner.
- **Verify (CONFIRMED):** Reproduced on all three axes. (1) No /lymphatic-glyfada route exists: `find src/app` shows only `src/app/lymphatic/page.tsx`; the area-lander pattern IS built for other services (chiropractic-glyfada, physiotherapy-glyfada, physiotherapy-voula/vari/vouliagmeni exist), so lymphatic was the omission. (2) legacy-redirects.ts:31 (`intensive-lymphatic-drainage-glyfada-gr.html`), :43 (vari variant), :52

### 🟡 MEDIUM · local-geo · ⚠️SEO — aggregateRating has no associated Review objects — star rich-result eligibility at risk
- **Πρόβλημα:** LocalBusiness carries a self-serving aggregateRating (5.0) with no accompanying 'review' array anywhere in the graph. Google requires aggregateRating to reference actual on-page review data and discourages self-serving LocalBusiness ratings without it; without Review objects the star snippet may not render and could be ignored/flagged. Meanwhile /reviews already contains 13 real, verbatim patient testimonials (QUOTES_EL/EN) rendered only as plain blockquotes — the raw material for valid Review markup exists but is not structured.
- **Απόδειξη:** src/lib/seo.ts:58-64 aggregateRating with no 'review' property; src/app/reviews/page.tsx:15-45 thirteen real testimonials rendered as <blockquote> with no Review schema
- **Fix:** Emit schema.org Review objects (author, reviewBody, reviewRating 5) for the /reviews testimonials and attach them (or the aggregateRating) to the #business entity, so the aggregateRating is backed by on-page reviews and eligible for star rich results.
- **Verify (CONFIRMED):** Independently reproduced. src/lib/seo.ts:58-64 attaches an aggregateRating (ratingValue "5.0") to the #business LocalBusiness entity with no 'review' property. A repo-wide grep for review/@type Review/reviewBody/reviewRating returns zero matches — there are no Review objects anywhere in the JSON-LD graph. src/app/reviews/page.tsx holds 13 real verbatim testimonials (QUOTES_EL/EN, lines 15-45) rend

### 🟡 MEDIUM · local-geo · ⚠️SEO — LocalBusiness sameAs omits the Google Business Profile / Maps URL and there is no map on the contact page
- **Πρόβλημα:** The LocalBusiness sameAs array lists only Instagram/Facebook/YouTube/TikTok (seo.ts:52-57) — it does not include the Google Business Profile / Google Maps place URL, and there is no hasMap property. Linking the site entity to its GBP place URL (via sameAs or hasMap) is a well-established local-SEO signal that reinforces the site↔GBP entity association driving the local pack. Additionally the /contact page shows NAP text but embeds no Google map and no 'get directions' link to the listing.
- **Απόδειξη:** src/lib/seo.ts:52-57 sameAs = socials only, no maps.app.goo.gl / maps.google URL; no hasMap in localBusinessSchema; src/app/contact/page.tsx has no map iframe or maps link (grep for iframe/maps returned nothing)
- **Fix:** Add the Google Business Profile place URL to LocalBusiness sameAs (and/or a hasMap property), and add a 'View on Google Maps / directions' link on the contact page to strengthen the GBP association.
- **Verify (CONFIRMED):** Reproduced independently. src/lib/seo.ts:52-57 sameAs contains only site.social.instagram/facebook/youtube/tiktok — no Google Business Profile or Google Maps place URL. localBusinessSchema has no hasMap property (grep for hasMap/maps.app.goo.gl/google.com/maps across all of src/ returned zero matches, so no GBP/Maps URL exists anywhere in the codebase). src/app/contact/page.tsx contains no map ifr

### 🟡 MEDIUM · local-geo · ⚠️SEO — Area-lander JSON-LD is hardcoded to Greek on the /en pages
- **Πρόβλημα:** Every area lander page component calls getAreaLander("el", SLUG) with the locale hardcoded to 'el' for its JSON-LD, while generateMetadata correctly uses the real locale. So on /en/physiotherapy-glyfada (and the other 6 EN area pages) the medicalProcedure name/description, the FAQPage questions/answers and the breadcrumb are emitted in GREEK even though the visible page and metadata are English. This degrades the English pages' structured-data/entity clarity and means the EN FAQ rich-result content mismatches the page language (and inherits the wrong Greek 10:00–22:00 hours too).
- **Απόδειξη:** src/app/physiotherapy-glyfada/page.tsx:15 export default Page() { const data = getAreaLander("el", SLUG) } — same pattern in all 7 area landers; contrast line 11 generateMetadata uses getAreaLander(await getLocale(), SLUG)
- **Fix:** Replace the hardcoded getAreaLander("el", SLUG) in each area-lander Page() with getAreaLander(await getLocale(), SLUG) so the JSON-LD language matches the page/URL locale.
- **Verify (CONFIRMED):** Reproduced in the repo. All 7 area-lander page components (physiotherapy-glyfada/voula/vari/vouliagmeni and chiropractic-glyfada/vari/vouliagmeni) hardcode getAreaLander("el", SLUG) in their default Page() (line ~16), which builds the JSON-LD — medicalProcedureSchema name/description (data.meta.title/description), faqSchema(data.faq), and breadcrumbSchema (literal "Αρχική"). Meanwhile generateMeta

### 🟡 MEDIUM · legal — Cookie/Privacy policy describes an analytics-consent banner that doesn't exist
- **Πρόβλημα:** The Cookie Policy (src/lib/legal.ts) states analytics/statistics cookies are used with consent ("Cookies στατιστικών (analytics): με τη συγκατάθεσή σας...") and section 3 states "Όταν επισκέπτεστε για πρώτη φορά τον ιστότοπο, εμφανίζεται ένα μήνυμα όπου μπορείτε να αποδεχθείτε ή να απορρίψετε τα μη απαραίτητα cookies." No cookie-consent banner exists anywhere (grep for consent/CookieConsent/gdpr in components finds only the booking-form field) and no analytics is installed. The policy also references storing the cookie-choice as a necessary cookie — that mechanism doesn't exist either. The published legal text materially misdescribes the site's actual behaviour, which is itself a GDPR/ePrivacy accuracy problem.
- **Απόδειξη:** src/lib/legal.ts cookies doc sections 2-3 (analytics-with-consent + first-visit banner); grep consent/CookieConsent/gdpr in src/components → none; no analytics lib installed
- **Fix:** Either (a) install analytics + a real consent banner that gates it and stores the choice, matching the copy; or (b) if launching with no analytics, rewrite the cookie policy to state only functional cookies (the locale cookie set by proxy.ts) are used and remove the banner/analytics claims.

### 🟡 MEDIUM · performance · ⚠️SEO — Every page served no-store — no CDN caching, worse TTFB / Core Web Vitals
- **Πρόβλημα:** Live response headers on /, /about and /booking all return `cache-control: private, no-cache, no-store, max-age=0, must-revalidate`. The proxy/middleware sets a locale cookie on every request (src/proxy.ts), which marks all responses uncacheable, so even static marketing pages (about, service landers) can never be served from Vercel's edge cache — every hit invokes the function. That raises TTFB globally, degrades the TTFB/LCP component of Core Web Vitals, and increases Vercel function usage. The old WordPress site is edge/page-cached, so this is a potential perceived-speed regression at cutover on pages Google measures for CWV.
- **Απόδειξη:** curl -I https://physiodanali.vercel.app/about and /booking → `cache-control: private, no-cache, no-store, max-age=0, must-revalidate`; src/proxy.ts sets locale cookie on every response
- **Fix:** Avoid setting a cookie on every request in middleware for anon marketing pages (only set locale cookie when it changes / on /en paths), or move locale detection to a cacheable mechanism, so static routes can return cacheable headers. Verify with a repeat curl that about/service pages return a cacheable cache-control.
- **Verify (ADJUSTED):** Symptom reproduced: curl -I on /, /about, /booking all return `cache-control: private, no-cache, no-store, max-age=0, must-revalidate` with `x-vercel-cache: MISS`, so pages are indeed uncacheable at the edge. Severity (medium, seoRisk) is fair as a TTFB/CWV concern. BUT the finding's stated mechanism and evidence are factually wrong: the proxy does NOT set a locale cookie on every request. src/pro

### 🟡 MEDIUM · seo-migration · ⚠️SEO — physiodanali.vercel.app is fully crawlable with canonicals to a domain serving different (old) content
- **Πρόβλημα:** Live headers on physiodanali.vercel.app carry no x-robots-tag and robots.txt Allow: / — the preview domain is fully indexable. Every page's canonical/OG/schema URL points to physiodanali.gr, which currently serves the OLD WordPress site (different content). Google crawling the vercel.app URLs sees a canonical pointing to a URL whose content doesn't match, so it may ignore the canonical and index physiodanali.vercel.app as a standalone duplicate of the new content — creating a duplicate-content cluster that competes with the old domain and can leak indexing signals before cutover.
- **Απόδειξη:** curl -I https://physiodanali.vercel.app/ → no x-robots-tag; robots.txt Allow: /; SITE_URL canonical = physiodanali.gr (still old WP site)
- **Fix:** Add a noindex (X-Robots-Tag or robots meta) that fires only for the *.vercel.app host until DNS cutover, then remove it the moment physiodanali.gr points at the new app. Alternatively keep the window very short and flip DNS immediately after deploy.
- **Verify (CONFIRMED):** Every claim reproduced against the live sites. (1) `curl -I https://physiodanali.vercel.app/` returns HTTP 200 with NO x-robots-tag header. (2) https://physiodanali.vercel.app/robots.txt returns `User-Agent: *` / `Allow: /` (only /api/ and /admin/ disallowed) — the preview domain is fully crawlable/indexable, and even declares `Host: https://physiodanali.gr` + sitemaps on the .gr domain. (3) The v

### ⚪ LOW · seo-migration · ⚠️SEO — /en/physical-therapy-at-home-gr.html 301s to Greek /home-care instead of /en/home-care
- **Πρόβλημα:** The stray EN-namespace URL with a -gr suffix (present in the old EN sitemap) redirects to the Greek /home-care page rather than /en/home-care. An English-indexed URL then lands users and Googlebot on Greek content, breaking language continuity and hreflang expectations for that redirect target.
- **Απόδειξη:** legacy-redirects.ts:66 { old: "/en/physical-therapy-at-home-gr.html", target: "/home-care" }; live curl confirms 308 -> /home-care. RECON D flagged this stray URL.
- **Fix:** Change the target to /en/home-care so the EN URL resolves to the EN page.
- **Verify (CONFIRMED):** Reproduced independently. legacy-redirects.ts:66 maps /en/physical-therapy-at-home-gr.html -> /home-care (Greek namespace). Live curl of https://physiodanali.vercel.app/en/physical-therapy-at-home-gr.html returns HTTP 308 with location: /home-care, landing an EN-namespace URL on the Greek page. The correct pattern is proven by the sibling at line 59 (/en/physical-therapy-at-home-eng.html) which re

### ⚪ LOW · technical-seo — el-only pages (/articles, /privacy, /cookies, /terms) emit canonical only, dropping languages/x-default hreflang
- **Πρόβλημα:** These pages set alternates:{canonical} in their own metadata, which shallow-overrides the layout's alternates, so they output no hreflang cluster (verified: live /articles has canonical but no alternate links). Acceptable because they have no English twin, but there is then no x-default declared for these URLs. Low impact.
- **Απόδειξη:** live curl https://physiodanali.vercel.app/articles shows <link rel=canonical> only, no <link rel=alternate>; recon: articles/privacy/cookies/terms set alternates:{canonical}
- **Fix:** Optional: leave as-is (single-language URLs do not require hreflang), or add languages:{el:path} + x-default:path for consistency.

### ⚪ LOW · on-page-seo — /en area-lander pages emit Greek-language JSON-LD (body hardcodes getAreaLander('el', …))
- **Πρόβλημα:** The area-lander page component calls getAreaLander('el', SLUG) with a hardcoded 'el' for its MedicalTherapy/FAQ/breadcrumb JSON-LD, so the English version (/en/physiotherapy-glyfada) emits Greek-language structured data even though its metadata is correctly localized. Cosmetic structured-data/locale mismatch, not lost SEO.
- **Απόδειξη:** src/app/physiotherapy-glyfada/page.tsx:16 default Page() calls getAreaLander("el", SLUG); generateMetadata (line 11) correctly uses await getLocale()
- **Fix:** Resolve the locale (await getLocale()) in the page body too so /en pages build their JSON-LD from the English lander data.

### ⚪ LOW · on-page-seo — Core condition landing pages (~1,000 words) are markedly thinner than the blog articles (~5,000 words)
- **Πρόβλημα:** The commercial condition pages that received 301s from old ranking pages are relatively thin: /low-back-pain 1,058 words, /neck-pain 1,080, /hip-pain 995. By contrast the informational articles run 4,000–6,500 words (/articles/disc-herniation 6,306, /articles/vestibular-rehab 6,546). The old treatment pages carried FAQPage schema and benefit-driven depth; the new condition pages do include FAQ+MedicalTherapy schema (good) but the body is light for a page absorbing a whole legacy sub-cluster's equity. Titles are strong and localized (e.g. 'Οσφυαλγία — Πρωτοποριακή εξατομικευμένη θεραπεία'). Not a loss risk on its own, but these pages will underperform their inbound equity until deepened, and they should interlink the matching rich article (e.g. /low-back-pain ↔ /articles/disc-herniation).
- **Απόδειξη:** live wordcounts: /low-back-pain 1058, /neck-pain 1080, /hip-pain 995 vs /articles/disc-herniation 6306, /articles/vestibular-rehab 6546
- **Fix:** Add 400–800 words (causes, treatment approach, home-visit specifics, an expanded FAQ) to /low-back-pain, /neck-pain, /hip-pain and cross-link each to its related deep article to distribute authority and capture more long-tail within the cluster.

### ⚪ LOW · functionality — No web manifest and no PNG/apple-touch icons — only favicon.ico
- **Πρόβλημα:** Live: /manifest.webmanifest and /site.webmanifest → 404; /icon.png, /apple-icon.png, /apple-touch-icon.png → 404; only /favicon.ico → 200. src/app has favicon.ico but no icon/apple-icon/manifest files. Saving the site to an iOS/Android home screen yields no proper icon, there is no theme-color/PWA metadata, and some SERP/social surfaces prefer a higher-res icon. Not SEO-critical but incomplete for a 100% launch.
- **Απόδειξη:** curl https://physiodanali.vercel.app/{manifest.webmanifest,icon.png,apple-icon.png,apple-touch-icon.png} → 404; favicon.ico → 200; src/app has only favicon.ico
- **Fix:** Add src/app/icon.png (512x512 or use the metadata icon convention), src/app/apple-icon.png (180x180), and a manifest (src/app/manifest.ts) with name/short_name/theme_color/icons.

### ⚪ LOW · accessibility — No skip-to-content link for keyboard users
- **Πρόβλημα:** site-chrome.tsx wraps children in a <main> landmark (good) and html lang is correctly el/en per locale, image alt coverage is good (decorative images use alt="" aria-hidden, content images have alt). But there is no skip-to-content link before the header nav, so keyboard/screen-reader users must tab through the full header on every page. Minor WCAG 2.4.1 gap.
- **Απόδειξη:** src/components/site/site-chrome.tsx:22 <main>{children}</main>; grep skip-link/skip-to-content in layout+site components → none
- **Fix:** Add a visually-hidden "Skip to content" anchor as the first focusable element in the layout, targeting the #main/<main> region.
